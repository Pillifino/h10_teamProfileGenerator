// Employee Card Super Class

class employeeCard {
    constructor(name, idNumber, email) {
        this.name = name; // initialize parameter
        this.idNumber = idNumber; // initialize parameter
        this.email = email; // initialize parameter
    }
}

module.exports = employeeCard;